{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<section class="container">
    <div class="row">
        <div class="col-3 align-center">
            <a href="{{ path('task_create') }}" class="btn btn-primary">Créer une nouvelle tâche</a>
        </div>
        <div class="col-4">
            <a href="{{ path('task_list') }}" class="btn btn-info">Consulter la liste des tâches à faire</a>        
        </div>
        <div class="col-5">
            <a href="{{ path('task_list_finished') }}" class="btn btn-secondary">Consulter la liste des tâches terminées</a>    
        </div>
    </div>
</section>
{% endblock %}
